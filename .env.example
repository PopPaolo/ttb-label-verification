# Copy to .env and fill in. Never commit the real .env.

# Required: authenticates label-extraction calls to the Anthropic API.
ANTHROPIC_API_KEY=

# Optional overrides (defaults shown; see internal/config/config.go).
#ADDR=:8080
#EXTRACTION_MODEL=claude-opus-4-8
#EXTRACTION_EFFORT=low
#BATCH_WORKERS=8
#BATCH_TTL=2h
#EXTRACTION_TIMEOUT=30s
#STATIC_DIR=web/dist
